
# ALX Travel App 0x01

## Objective
Build API views to manage listings and bookings, and ensure endpoints are documented with Swagger.

## Setup
```bash
git clone <your repo>
cd alx_travel_app_0x01
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
